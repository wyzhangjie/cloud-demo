<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.opay.payment.activity.provider.server.repository.RulesRepository">
    <resultMap id="BaseResultMap" type="com.opay.payment.activity.provider.server.entity.ActivityRules">
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="activity_id" property="activityId" jdbcType="VARCHAR" />
        <result column="activity_condition_key" property="activityConditionKey" jdbcType="VARCHAR" />
        <result column="activity_condition_value" property="activityConditionValue" jdbcType="VARCHAR" />
        <result column="snapshot" property="snapshot" jdbcType="VARCHAR"/>
        <result column="condition_status" property="conditionStatus" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
    </resultMap>
    <sql id="Base_Column_List">
    `id`,`snapshot`, `activity_id`, `activity_condition_key`, `activity_condition_value`, `condition_status`, `create_time`, `update_time`
  </sql>

    <insert id="insertBatch" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="id">
        insert into `activity_rules` (`activity_id`, `snapshot`,`activity_condition_key`,
        `activity_condition_value`, `condition_status`
       )
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.activityId,jdbcType=VARCHAR},  #{item.snapshot,jdbcType=VARCHAR}, #{item.activityConditionKey,jdbcType=VARCHAR},
            #{item.activityConditionValue,jdbcType=VARCHAR}, #{item.conditionStatus,jdbcType=VARCHAR})
        </foreach>
    </insert>

    <update id="delActivityRules" parameterType="com.opay.payment.activity.provider.server.entity.Activity">
    update `activity_rules`
    set
      `condition_status` = 'N',
      `update_time` = #{updateTime,jdbcType=TIMESTAMP}
    where 1=1
    and   activity_id = #{activityId}
  </update>
    <update id="onLineRules">
    update `activity_rules`
    set
      `condition_status` = 'Y'
    where 1=1
    and   activity_id = #{activityId}
    and snapshot = #{snapshot}
  </update>
    <select id="findRulesByActivityIdAndSnapshot" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from `activity_rules`
        where 1=1
        <if test="activityId != null and activityId != ''">
            and activity_id = #{activityId}
        </if>
        <if test="snapshot != null and snapshot != ''">
            and snapshot = #{snapshot}
        </if>
    </select>

</mapper>